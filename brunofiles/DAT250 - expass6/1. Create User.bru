meta {
  name: 01 - Create User
  type: http
  seq: 1
}

post {
  url: {{url}}/users
  body: json
  auth: none
}

body:json {
  {
    "username": "testuser",
    "email": "test@test.com",
    "password": "test123"
  }
}

tests {
  test("User created", function() {
    expect(res.status).to.equal(200);
  });
}
