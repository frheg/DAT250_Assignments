package com.example.PollApp;

import org.springframework.stereotype.Component;

/**
 * The DomainManager should be annotated with the @Component annotation, 
 * which will allow to inject it into the controller classes.
 */

@Component
public class DomainManager {
    
}
